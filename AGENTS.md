# AGENTS.md — Datapass Control

## Global authority first

Before using this repository, read `julian-passebecq/dataprojects`.

- `dataprojects` owns the current global constellation, six-product boundary, toolbox/IDE categories and portfolio status.
- This repository is a detailed Datapass-family sub-registry/history.
- Product repositories remain authoritative for implementation truth.
- If this repo's older four-product language conflicts with `dataprojects`, follow `dataprojects`.

Then read:

1. `control/index.json`
2. relevant `control/projects/<product>.json`
3. `control/relationships.json`
4. `control/shared-assets.json`
5. relevant `control/decisions/`
6. `control/workstreams.json`

Do not create another global control database from this repository. Preserve current-vs-target truth and reuse donor assets before deleting them.
